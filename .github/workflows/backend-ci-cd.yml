name: Backend CI/CD

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -t starttech-backend .

      # Commented out the scan for now to isolate the build error
      # - name: Scan for vulnerabilities
      #   uses: aquasecurity/trivy-action@master
      #   with:
      #     image-ref: 'starttech-backend:latest'
      #     format: 'table'
      #     exit-code: '1'